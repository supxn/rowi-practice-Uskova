export interface TypeData{
    nickname: string;
    FIO: string;
    number: string;
    email: string;
    password: string;
}
export interface InputFields { 
    title: string;
    status: boolean;
}